#include <bits/stdc++.h>
using namespace std;
bool isHere(int arr[], int size, int n){
    for (int i = 0; i < size; i++){
        if (arr[i] == n){
            return true;
        }
    }
    return false;
}

int main()
{
    int n, m;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++){
        cin >> arr[i];
    }

    cin >> m;
    int arrB[m];
    int count = 0;
    for (int i = 0; i < m; i++){
        cin >> arrB[i];
    }
    int dif[n];
    for (int i = 0; i < n; i++){
        bool notExist = true;
        for (int j = 0; j < m; j++){
            if (arr[i] == arrB[j]){
                notExist = false;
                break;
            }
        }

        if (notExist == true && isHere(dif, n, arr[i]) == false){
            dif[count] = arr[i];
            count++;
        }
    }
    for (int i = 0; i < count; i++){
        cout << dif[i] << " ";
    }

    return 0;
}
